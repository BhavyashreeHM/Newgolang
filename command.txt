commands
git init
   create git but it is hidden store

change branch
git config --global init.defaultBranch main (from master )
  or
git branch -m boss  (boss is name of the branch)

git status - change the status of 

git add .   -  add the recent changes no commit - it means staged
   (. - many files
    name.txt - single file for ex: git add name.txt )

git commit -m "command.txt is command file"    - commit     
           shows on branch new( new is branch name it may show anything)
           nothing to commit, wokring tree is free

git log = it shows whatever you did


this 2 commands add local to github repo
git remote add origin https://github.com/BhavyashreeHM/Newgolang.git   -
 git push -u origin new - this is add local to github repository
            
